<h1>Available Sources</h1>

<% @sources.each do |s| %>
    <p><%= s.name %> -- <em><%= s.location %></em><br>
    <% if s.fee == true %>
    There is a fee.
    <% end %>
    </p>
<% end %>

<p><%= link_to "Only Free Sources", free_sources_path %><br>
<%= link_to "Only Paywalled Sources", paywall_sources_path %><br>
<%= link_to "Add a Source", new_source_path %></p>